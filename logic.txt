Question
   ↓
Local FAISS RAG
   ↓
Ollama / llamafile
   ↓
is_related?
   ├── false → stop
   └── true
         ↓
      JSON variable
         ↓
      Ollama / llamafile
         ↓
 Generate Python code
 SQL embedded inside the Python code
         ↓
      AutoGen
 Execute the Python code
         ↓
   success → result
         ↓ error
 question + RAG + JSON + code + error
         ↓
 Ollama / llamafile fixes the code
         ↓
 AutoGen executes the code again
